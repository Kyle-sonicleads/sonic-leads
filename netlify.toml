[build]
  command = "npm install -g pnpm && pnpm install && pnpm --filter @workspace/sonic-leads run build"
  publish = "artifacts/sonic-leads/dist/public"

[build.environment]
  NODE_VERSION = "20"
  BASE_PATH = "/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
